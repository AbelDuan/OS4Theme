<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name">

        <!-- LibXposed 模块声明在 resources/META-INF/xposed/（module.prop / java_init.list /
             scope.list），不再使用 manifest meta-data（那是 XposedBridge 82 时代的做法） -->

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 日志通道：SystemUI 进程推送日志到模块私有目录（设置开关由 LibXposed
             框架直供，不再经此 provider）。
             directBootAware：用户解锁前（Direct Boot 阶段）也可拉起本 provider，
             读取 DE 存储的设置 → 重启后通知下沉零空窗 -->
        <provider
            android:name=".StatusProvider"
            android:authorities="com.abel.hyperosglass.status"
            android:exported="true"
            android:directBootAware="true" />

        <!-- 导出日志时以 content:// 形式只读分享日志文件（query 返回文件信息，
             澎湃互联等接收端才能正常读取） -->
        <provider
            android:name=".LogFileProvider"
            android:authorities="com.abel.hyperosglass.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>

</manifest>
